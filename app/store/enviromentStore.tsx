/* eslint-disable prettier/prettier */
import { action, autorun, observable, configure, values } from 'mobx';
import { observer } from 'mobx-react'


configure({ enforceActions: 'observed' });
const URL = '';
export const enviromentStore = observable({
  

}, {
  

});